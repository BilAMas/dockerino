combine the:
- build file 
- dev file 
- exec once the container is up and running file
    docker exec -it elixir

add user to the docker image - sort out the read only thing once i  am out of the container



maybe a script for clearing out and kill containers
docker container prune 
docker container kill CONTAINER NAMES
